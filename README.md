# Castle Wars

Кооперативная игра на выживание с авторитарным сервером, ECS архитектурой и WebGL клиентом.

## Архитектура

- **Сервер**: Node.js + TypeScript + Colyseus + ECS
- **Клиент**: PixiJS + TypeScript + Vite
- **Сетевая модель**: Авторитарный сервер (20-30 TPS)
- **Клиентский рендер**: 60 FPS с prediction и interpolation

## Структура проекта

```
castle_wars/
├── server/          # Colyseus сервер с ECS
│   ├── src/
│   │   ├── ecs/     # ECS система (компоненты, системы, мир)
│   │   ├── rooms/   # Colyseus комнаты
│   │   └── schemas/ # Colyseus схемы состояния
│   └── Dockerfile
├── client/          # PixiJS клиент
│   ├── src/
│   │   ├── GameClient.ts
│   │   └── schemas/
│   └── Dockerfile
└── docker-compose.yml
```

## Быстрый старт

### С Docker Compose (рекомендуется)

```bash
# Запустить все сервисы в режиме разработки (с hot reload)
docker-compose up --build

# Сервер будет доступен на ws://localhost:2567
# Клиент будет доступен на http://localhost:8080

# Остановить сервисы
docker-compose down
```

**Примечание**: Docker Compose использует dev-режим с hot reload. Для продакшена используйте отдельные Dockerfile файлы.

### Локальная разработка

#### Сервер

```bash
cd server
npm install
npm run dev
```

Сервер запустится на `ws://localhost:2567`

#### Клиент

```bash
cd client
npm install
npm run dev
```

Клиент будет доступен на `http://localhost:8080`

## Управление

- **WASD** или **Стрелки** - движение персонажа
- Игроки видят друг друга в реальном времени
- Движение синхронизируется через сервер

## Этапы разработки

### ✅ Этап A - Сетевое ядро (текущий)
- [x] Базовая ECS структура
- [x] Colyseus интеграция
- [x] Движение персонажей
- [x] Синхронизация состояния

### 🔄 Этап B - Взаимодействие с миром
- [ ] Столкновения
- [ ] Объекты окружения
- [ ] Сбор ресурсов

### 📋 Этап C - Кооперативные механики
- [ ] Базовые мобы
- [ ] Совместный бой
- [ ] Revive / поддержка

### 📋 Этап D - Устойчивость
- [ ] Reconnection
- [ ] Rate-limit инпута
- [ ] Cheat prevention

## ECS Компоненты (MVP)

- `Position` - позиция в мире
- `Velocity` - скорость движения
- `InputController` - обработка ввода игрока
- `NetworkReplication` - флаг для синхронизации

## ECS Системы (MVP)

- `InputSystem` - применение команд игроков
- `MovementSystem` - физика перемещения

## Технологии

- **Colyseus** - WebSocket сервер для мультиплеерных игр
- **PixiJS** - WebGL рендеринг
- **TypeScript** - типизированный JavaScript
- **Docker** - контейнеризация

## Лицензия

MIT

